package lava.vm;

/**
 * Represents a location that is used as an input in the VM interface.
 */
public interface VMInputLocation {
    /**
     * Return index where this is stored.
     */
    int getIndex();
}
