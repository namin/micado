package lava.vm;

/**
 * Represents a location that is used as an output in the VM interface.
 */
public interface VMOutputLocation {
    /**
     * Return index where this is stored.
     */
    int getIndex();
}
